package com.jmeter.autocorrelator;

import org.apache.jmeter.config.ConfigTestElement;
import org.apache.jmeter.testelement.TestElement;

// This acts as the anchor for our GUI in the JMeter tree
public class AutoCorrelatorElement extends ConfigTestElement {
    // Empty because the logic is entirely handled by the GUI actions
}